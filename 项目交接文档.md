# Marstek Home Assistant Integration - 项目交接文档

## 项目概述

这是一个用于Home Assistant的Marstek储能设备集成，支持通过UDP协议与Marstek设备进行通信，实现设备状态监控和远程控制。

## 项目结构

```
ha_marstek/
├── config/custom_components/marstek/     # 主要集成代码
│   ├── __init__.py                       # 集成入口，服务注册
│   ├── config_flow.py                    # 配置流程
│   ├── device_action.py                  # 设备动作（充电/放电/停止）
│   ├── sensor.py                         # 传感器平台，数据轮询
│   ├── switch.py                         # 开关平台，设备控制
│   ├── udp_client.py                     # UDP通信客户端
│   ├── command_builder.py                # 命令构建器
│   ├── const.py                          # 常量定义
│   ├── manifest.json                     # 集成清单
│   ├── logo.png                          # 集成图标
│   └── services.yaml                      # 服务定义
├── README.md                             # 项目说明
└── HANDOVER.md                           # 本交接文档
```

## 核心功能

### 1. 设备发现与连接
- **UDP广播发现**：自动发现网络中的Marstek设备
- **设备信息获取**：获取设备IP、MAC、版本等信息
- **连接管理**：维护设备连接状态

### 2. 数据轮询机制
- **轮询间隔**：每10秒轮询一次设备状态
- **轮询控制**：支持暂停/恢复轮询，避免并发冲突
- **数据缓存**：30秒设备发现缓存

### 3. 设备控制
- **充电控制**：支持设置充电功率（默认-1300W）
- **放电控制**：支持设置放电功率（默认1300W）
- **停止控制**：停止充放电操作
- **模式切换**：支持手动/自动模式切换

### 4. 传感器数据
- **电池电量**：SOC百分比
- **功率显示**：充放电功率
- **设备模式**：运行模式状态
- **电池状态**：充电/放电/空闲状态

## 关键特性

### 轮询控制机制

```python
# 在自动化触发时自动暂停轮询
await udp.send_request_with_polling_control(command, host, port, timeout=2.0)
```

**工作流程**：
1. 触发自动化动作时暂停设备轮询
2. 发送控制命令（2秒超时，最多5次重试）
3. 无论成功失败都恢复轮询

### 重试机制
- **超时时间**：2秒
- **重试次数**：最多5次
- **重试间隔**：2秒

## 设备通信协议

### UDP通信
- **端口**：30000（默认）
- **协议**：JSON格式的UDP消息
- **命令类型**：
  - `ES.GetMode`：获取设备状态
  - `ES.SetMode`：设置设备模式
  - `discover`：设备发现

### 消息格式
```json
{
  "id": 0,
  "config": {
    "mode": "Manual",
    "manual_cfg": {
      "time_num": 0,
      "start_time": "00:00",
      "end_time": "23:59",
      "week_set": 127,
      "power": -1300,
      "enable": 1
    }
  }
}
```

## 开发环境设置

### 1. 环境要求
- Python 3.13
- Home Assistant 2023.1+
- 网络访问权限

### 2. 搭建开发环境

#### 本地开发环境搭建
```bash
# 1. 创建虚拟环境
python3.13 -m venv venv

# 2. 激活虚拟环境
source venv/bin/activate

# 3. 安装依赖和设置开发环境
script/setup
```

#### 运行 Home Assistant（开发模式）
```bash
# 1. 设置开发环境
script/setup

# 2. 停止现有的Home Assistant进程
pkill -f "hass -c config"

# 3. 启动Home Assistant开发模式
hass -c config

# 4. 访问Home Assistant
# 浏览器打开：http://localhost:8123

```

### 3. 代码获取和部署

#### Git拉取最新代码
```bash
# 克隆项目（首次）
git clone http://192.168.0.230/XieVictory/ha_marstek.git
cd ha_marstek

# 切换到最新分支,目前最新分支是v0，之后开发版本号注意递增
git checkout v0

# 拉取最新代码
git pull origin v0
```

#### 项目部署到Home Assistant服务器
```bash
# 在本地开发机器上执行（将集成文件复制到HA服务器）
scp -r ./config/custom_components/marstek root@<ha-ip>:/root/config/custom_components/

# 说明：
# - 在本地开发机器上运行此命令
# - <ha-ip>替换为Home Assistant服务器的实际IP地址
# - 例如：scp -r ./config/custom_components/marstek root@192.168.3.11:/root/config/custom_components/
```

### 4. 开发调试

#### 启用调试日志
```yaml
# 在Home Assistant的configuration.yaml中添加
logger:
  default: info
  logs:
    custom_components.marstek: debug
    custom_components.marstek.udp_client: debug
    custom_components.marstek.sensor: debug
    custom_components.marstek.device_action: debug
```

#### 开发工作流程
```bash
# 1. 本地开发
# 修改代码后，重启Home Assistant开发实例
pkill -f "hass -c config"
hass -c config

# 2. 测试完成后，部署到生产环境
scp -r ./config/custom_components/marstek root@?ip:/root/config/custom_components/

# 3. 在生产服务器上重启Home Assistant

```


## 代码关键点

### 1. UDP客户端 (`udp_client.py`)
- **轮询控制**：`pause_polling()`, `resume_polling()`
- **请求发送**：`send_request_with_polling_control()`
- **设备发现**：`discover_devices()`

### 2. 传感器平台 (`sensor.py`)
- **数据轮询**：`MarstekDataUpdateCoordinator`
- **轮询检查**：`is_polling_paused()`
- **数据更新**：`_async_update_data()`

### 3. 设备动作 (`device_action.py`)
- **重试机制**：5次重试，2秒间隔
- **轮询控制**：自动暂停/恢复
- **错误处理**：超时和网络错误处理


## 快速开始指南


1. **环境准备**
   ```bash
   # 克隆项目
   git clone http://192.168.0.230/XieVictory/ha_marstek.git
   cd ha_marstek
   git checkout v0
   
   # 创建虚拟环境
   python3.13 -m venv venv
   source venv/bin/activate
   
   # 设置开发环境
   script/setup
   ```

2. **启动开发环境**
   ```bash
   # 停止现有HA进程
   pkill -f "hass -c config"
   
   # 启动HA开发模式
   hass -c config
   
   # 访问 http://localhost:8123
   ```

3. **部署到生产环境**
   ```bash
   # 在本地开发机器上执行
   scp -r ./config/custom_components/marstek root@<ha-ip>:/root/config/custom_components/
   
   # 在HA服务器上重启服务
   systemctl restart home-assistant
   ```

### 重要说明

- **<ha-ip>替换说明**：将 `<ha-ip>` 替换为实际的Home Assistant服务器IP地址
- **scp命令执行位置**：在本地开发机器上执行，不是HA服务器上
- **最新代码分支**：v0分支目前包含最新的开发代码，版本号递增，比如v1，v2，以此类推
- **访问地址**：开发环境默认是 http://localhost:8123

---

